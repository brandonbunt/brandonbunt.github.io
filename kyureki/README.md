## Public Interface

This repository publishes:
- `/data.json` – daily kyūreki + rokuyō data (UTC-based)
- `/images/` – static seasonal imagery
- `/render/` – optional pre-rendered images (best-effort)

Clients should not assume internal implementation details.
